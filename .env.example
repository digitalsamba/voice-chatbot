# Required for OpenAI API access
OPENAI_API_KEY=your_openai_api_key_here

# Server port configuration (default: 3011)
PORT=3011

# Environment mode (development or production)
NODE_ENV=development

# Optional: Janus server configuration
# Only required if you're using a custom Janus server setup
# JANUS_SERVER_URL=ws://localhost:8188/